package anjani.fucntions;

public class Add {
    public int addPositiveNumbers(int thisNumber, int thatNumber) {
        return (thisNumber > 0 && thatNumber > 0) ? thisNumber + thatNumber : 0;
    }
}
